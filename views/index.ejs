<%- include('layouts/minimal') %>

<% 
if (typeof nav_enabled === 'undefined') {
  var nav_enabled = true;
}
if (typeof has_children === 'undefined') {
  var has_children = true;
}
%>

<div class="row">
  <div class="col-lg-8">
    <div class="card">
      <div class="card-header">
        <h1 class="card-title mb-0">Selamat Datang di Partitur</h1>
      </div>
      <div class="card-body">
        <p class="card-text">
          Koleksi partitur musik gerejawi dalam format digital interaktif. 
          Pilih lagu dari daftar di bawah untuk memulai.
        </p>
        
        <% if (songs && songs.length > 0) { %>
          <div class="song-list">
            <h3>Daftar Lagu</h3>
            <div class="list-group">
              <% songs.forEach(function(song) { %>
                <a href="<%= song.url %>" class="list-group-item list-group-item-action">
                  <div class="d-flex w-100 justify-content-between">
                    <h5 class="mb-1"><%= song.title %></h5>
                    <small class="text-muted"><%= song.instrument %></small>
                  </div>
                  <p class="mb-1"><%= song.workType %></p>
                  <small>🎵 <%= song.title %></small>
                </a>
              <% }); %>
            </div>
          </div>
        <% } else { %>
          <div class="alert alert-warning">
            <h4>⚠️ Tidak ada lagu ditemukan</h4>
            <p>Pastikan direktori 'lagu' ada dan berisi file konfigurasi yang valid.</p>
          </div>
        <% } %>
      </div>
    </div>
  </div>
  
  <div class="col-lg-4">
    <div class="card">
      <div class="card-header">
        <h5>Tentang Partitur</h5>
      </div>
      <div class="card-body">
        <p class="card-text">
          Proyek ini menyediakan partitur musik gerejawi dalam format digital yang interaktif.
        </p>
        <ul class="list-unstyled">
          <li>📝 Notasi musik digital</li>
          <li>🎵 Pemutar audio sinkron</li>
          <li>🎨 Visualisasi interaktif</li>
          <li>📱 Responsif untuk mobile</li>
        </ul>
        </div>
    </div>
    
    <div class="card mt-3">
      <div class="card-header">
        <h5>Teknologi</h5>
      </div>
      <div class="card-body">
        <p class="card-text">
          Dibangun dengan teknologi modern:
        </p>
        <ul class="list-unstyled">
          <li>🚀 NodeJS & Express</li>
          <li>🎨 EJS Templating</li>
          <li>📱 Bootstrap CSS</li>
          <li>🎵 Archive.org CDN</li>
        </ul>
      </div>
    </div>
  </div>
</div>
